"""Packaged prompt templates for ``story-develop``.

This package marker exists so the templates are loadable via
``importlib.resources.files`` regardless of install mode (a bare directory works
as a namespace package today, but an explicit package removes the ambiguity).
"""
